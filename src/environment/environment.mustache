export const environment: {
  baseUrl: string;
  auth: {
    domain: string;
    clientId: string;
    scope: string;
  };
} = {
  baseUrl: "{{BASE_URL}}",
  auth: {
    domain: "{{&COGNITO_DOMAIN}}",
    clientId: "{{COGNITO_CLIENT_ID}}",
    scope: "{{COGNITO_SCOPES}}"
  }
};
